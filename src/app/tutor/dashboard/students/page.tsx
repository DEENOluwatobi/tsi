'use client';
import React, { JSX } from 'react';
import StudentsPage from '@/features/tutor/Students';

const Page = (): JSX.Element => {

    return (
        <div>
            <StudentsPage />
        </div>
    );
};

export default Page;