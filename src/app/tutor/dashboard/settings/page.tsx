'use client';
import React, { JSX } from 'react';
import Settings from '@/features/tutor/Settings';

const Page = (): JSX.Element => {
    return (
        <div>
            <Settings/>
        </div>
    );
};

export default Page;